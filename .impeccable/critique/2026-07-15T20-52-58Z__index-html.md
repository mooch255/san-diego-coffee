---
target: index.html (homepage)
total_score: 22
p0_count: 0
p1_count: 3
timestamp: 2026-07-15T20-52-58Z
slug: index-html
---
Method: dual-agent (A: af2720bcc9caafd28 · B: a39f0e7e975e51d06)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Hero stats at `—` until deferred JS; sections pop in, no skeletons |
| 2 | Match System / Real World | 2 | "23 Specialty Cafes" contradicts the page's own "200+ specialty cafes" |
| 3 | User Control and Freedom | 3 | Drawer handles Esc/close/backdrop; 320ms transition blackout inescapable |
| 4 | Consistency and Standards | 2 | Two identical "VIEW ALL" buttons -> unrelated places; .roaster/.cafe same colour |
| 5 | Error Prevention | 3 | Newsletter: regex validation, honeypot, novalidate + custom handling |
| 6 | Recognition Rather Than Recall | 2 | All 8 destinations behind hamburger at 1400px; brand absent above fold |
| 7 | Flexibility and Efficiency | 2 | No search on a 210-item directory; JSON-LD SearchAction promises one |
| 8 | Aesthetic and Minimalist | 2 | 12-link farm in intro; featured news slot is a cropped white vendor logo |
| 9 | Error Recovery | 2 | Single string: "Something went wrong. Try again." |
| 10 | Help and Documentation | 2 | No curation methodology on the page that must earn trust |
| **Total** | | **22/40** | **Acceptable** |

## Anti-Patterns Verdict

Passes "did AI make this"; fails "how was this made?". Real local imagery, committed pre-existing palette, measured contrast ratios in tokens. Drifts generic in the back half: three consecutive card grids, identical hover treatments. Biggest gap: the page is about a map of a city and contains no map, no pins, no geography.

Deterministic scan: CLI exit 2, 1 finding — layout-transition at index.html:436 (`transition: padding-left` on nav drawer hover). Browser in-page run: 11 findings, 9 false positives (detector's non-visual contrast path cannot composite the stacked photo + gradient scrim; reported the hero as white-on-white 1.0:1). No visual overlay — headless only.

## CORRECTION: Assessment A error

A reported loc_229/loc_230 as a duplicate record and advised merging at source. VERIFIED FALSE — different addresses (4440 Twain Ave vs 6606 Mission Gorge Rd), different coords, different Google placeIds. Two real locations. 31 duplicate names exist in locations.js; nearly all are legitimate multi-location brands (Bird Rock 11, Lofty 6). Merging would destroy data.
The UX half survives and is live: What's New currently renders Tialli, then CUPPA CUPPA/Allied Gardens, then CUPPA CUPPA/Allied Gardens — two visually identical cards. Fix is display-side dedupe, not a merge.

## Priority Issues

[P1] Stats don't reconcile. 210 Locations / 115 Roasters / 23 Specialty Cafes; 115+23=138. Units mixed: 210 = rows, 115/23 = deduped brands via normalizeBrandName(). "23 Specialty Cafes" contradicts the meta description "200+ specialty cafes". type field is 183 roaster / 27 cafe — miscategorised upstream. JS-off footer fallback says "150+" while static copy says "200+". Three counts on one page. Audience is "coffee data nerds". Fix: unify units or relabel; fix type upstream; update 150+ fallback.

[P1] No map on a map site. 210 pins, 50+ neighborhoods, none shown. CTA says "Explore the Full Map" implying a partial one exists. /og-image.png is a stylized SD map with cup-pins shown only to Twitter. Fix: inline og-image.svg as a section, or a static-tile render with pins.

[P1] Desktop nav entirely behind a hamburger at 1400px; .header-brand display:none over hero so the site is unnamed above the fold. Nobody discovers Roaster Stories (contains Yipao, the best content). .nav-btn styles exist and are hidden at <=768px only. Fix: above 1024px show the brand lockup + 4-5 inline links.

[P2] Hero doesn't commit. clamp(2.1rem, 3.2vw, 3rem) = 48px at 1400px; mobile 40px — a 2.6x viewport increase buys 8px. Register ceiling is 6rem. Indecisive crop (newsstand sign + figure's back compete with the roaster drum). Bottom 40% empty at 1024. .hero-subtitle 0.9rem is smaller than the body copy below it.

[P2] Intro block is a link farm. 9 neighborhood links in one run-on sentence, 12 across the block. Reads as a doorway page at peak visitor interest. Fix: render as a visual index with counts; doubles as the missing geography.

## Persona Red Flags

Jordan: can't tell what site he's on at 1400px; never discovers Guides/Roaster Stories; "23 cafes" vs "200+ spots" breaks his mental model; 320ms blackout reads as a crash; sees CUPPA CUPPA twice.
Riley: does the arithmetic in seconds; JS-off gives three different counts; finds 1 location silently dropped by .filter(l => l.metadata?.importDate); two identical VIEW ALL buttons -> unrelated destinations.
Casey: --latte stat labels land on the neon sign at 540px; all nav is a top-right reach; intro is a screen of prose with 12 wrapping tap targets; no search. Credit: 44pt+ targets, <picture> mobile asset.

## Minor Observations

- .recent-card-type.roaster and .cafe both var(--coffee-brown) — two classes, one colour.
- Featured news card is a white logo box; imageFit:contain crops "coffee" off the Klatch lockup in the largest slot on the page.
- .news-card-image height 66px on non-featured cards.
- --section-pad defined, used once; all other sections hardcode padding.
- .header-eyebrow lockup is the best small typography on the site and is hidden until .scrolled.
- Lora in exactly 2 rules, both adjacent to Playfair; two transitional serifs, invisible distinction, third font family per load.
- 18 text-transform: uppercase declarations — one register for labels, badges, CTAs and nav; hierarchy reads flat.

## Questions to Consider

1. The OG image is a stylized SD map with cup-pins that only Twitter sees. Why is the best asset pointed away from the homepage?
2. The Yipao block is the only thing with a POV — why is it at 70% scroll depth between two card grids?
3. Audience is "coffee data nerds who want roast profiles" — where is one roast profile on this page?
4. Every small text element is uppercase and letterspaced. What is the quiet voice?
